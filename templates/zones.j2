# {{ ansible_managed }}
#
# For information about entries in this file, type "man shorewall-zones"
###############################################################################
#ZONE     TYPE           OPTIONS               IN                      OUT
#                                              OPTIONS                 OPTIONS

{% for zone in elao_shorewall_config.zones %}
{{ zone.name }}{% for i in range(10 - zone.name|length) %} {% endfor %}
{{- zone.type }}
{% endfor %}

